# Renderer Rebuild Grounding Proof Audit

**Date:** 2026-08-27  
**Branch:** `refactor/renderer-rebuild`  
**Author:** Manus AI  
**Status:** First web proof slice complete; owner visual approval required before expansion

## Scope and decision

This audit records the first implementation slice of the approved renderer rebuild. It does **not** replace the existing merged Pixi or React Native Skia renderers. Instead, it introduces an isolated web route, `?renderer=rebuild&demo=grounding`, that uses the unchanged shared `GameEngine` state as input but renders a new shallow rear-chase composition in Canvas 2D. The former renderer remains the default production route and `?renderer=canvas2d` remains its manual fallback; neither has been deleted or changed as part of this proof.

The rewrite responds to a demonstrated visual failure in the earlier elevated-deck implementation: road geometry, vehicle sprites, shadows, and effects applied related but separate position rules. This created visible disagreement about where a vehicle touched the road. The proof slice gives every rendered vehicle one `GroundPose` generated by a pure `buildRebuildFrame()` module. The road width, road row, vehicle scale, tire contact row, contact shadow, and wet-road reflection are all derived from that same pose.

## Implemented architecture

| Component | Responsibility | Present in proof |
| --- | --- | --- |
| `rebuild-frame.ts` | Pure screen-space ground/contact poses from immutable `GameState`; no gameplay mutation. | Yes |
| `rebuild-renderer.ts` | Stable Canvas 2D chase-camera scene, road, skyline, rails, vehicle layering, shadows, and reflections. | Yes |
| `Game.tsx` | Selects `?renderer=rebuild`; `demo=grounding` freezes the initial engine state for a reproducible approval frame. | Yes |
| Compact vehicle textures | Three keyed RGBA rear/front chase vehicle textures; source masters remain ignored. | Yes |
| Legacy Pixi scene and native Skia scene | Existing production/rollback renderers. | Retained, unmodified by this proof |

The implementation has no changes to `lib/game-core`, movement, collision geometry, spawn rules, score, Rush, control handling, audio, or backend state. Normal gameplay continues to start the shared engine. Only the explicit `demo=grounding` route omits `engine.start()` so a screenshot cannot be replaced by a collision/game-over state before review.

## Evidence

The capture below was obtained from the isolated browser at the exact 420×800 viewport using the proof URL after Start Engine. The capture uses the static approval mode, so `SCORE`, `DIST`, and speed stay at their initial values; that behavior is deliberate and is not a playable mode.

![Renderer rebuild grounding proof](2026-08-27_Manus_RendererRebuildGroundingProof.png)

In the recorded frame, the player, same-direction traffic, and oncoming traffic all recede or enlarge along the same cyan-railed road corridor. The player’s actual image baseline ends at the compact shadow/reflection row, while traffic positions and scale converge toward the horizon. This replaces the prior independent, flat/sprite-center placement with one road-plane contact contract.

## Asset contract

The runtime proof uses three compact RGBA vehicle textures documented in [`docs/RENDERER_REBUILD_ASSETS.md`](../docs/RENDERER_REBUILD_ASSETS.md). Their high-resolution generated source art is intentionally ignored under `rebuild-assets/`; only the optimized web runtime textures are versioned. Assets carry no baked road, shadow, reflection, atmospheric layer, or text. Chroma-key conversion and alpha trimming remove source backdrops before runtime use.

## Validation record

| Check | Result | Evidence or limitation |
| --- | --- | --- |
| Frontend TypeScript | Passed | `pnpm --filter @workspace/warboss-highway run typecheck` |
| Full workspace TypeScript | Passed | `pnpm run typecheck` |
| Whitespace validation | Passed | `git diff --check` |
| Isolated 420×800 visual capture | Passed | Screenshot above; 0 browser console errors at route navigation/start. |
| Normal legacy default renderer | Preserved by code path | Not re-profiled in this proof pass. |
| Physical iOS/Android runtime | Not run | No emulator or device profiler was available. |
| Hardware WebGL | Not evaluated | The rebuild proof is Canvas 2D and is not a WebGL performance claim. |
| Native Skia renderer rebuild | Not started | Deferred until owner accepts this web proof composition. |
| Remote CI/PR | Not started | This proof branch is intentionally uncommitted/unpushed at audit time. |

## Explicit exclusions

The first proof slice deliberately excludes rain, lightning, billboards, puddle fields, boss rendering, obstacles, pickups, screen-shake treatment, Rush visuals, high-speed motion capture, accessibility review, runtime culling under dense traffic, native Skia parity, and TestFlight distribution. None of these are represented as complete. They must be layered only after the owner accepts this frame’s camera, car-scale, and road-contact composition.

## Next approval gate

The required owner decision is whether the **camera pitch, realistic rear-chase vehicle scale, road-plane contact, and overall road corridor** are now a credible foundation. A visual acceptance permits the next bounded phase: apply the same pure visual frame to real web state, add a motion proof, then implement a matching native adapter. A rejection retains the legacy implementation as rollback and prompts an adjustment to this isolated proof branch; no existing renderer will be removed without explicit approval.
